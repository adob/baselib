A general purpose library for C++ with a Go-style API.

Work-in-progress.

CMake build check:

rm -rf /tmp/baselib-cmake-check/; cmake -S . -B /tmp/baselib-cmake-check && cmake --build /tmp/baselib-cmake-check
